# Licence record — country flags

- **Source**: `lipis/flag-icons`, `flags/4x3/` (249 SVG, the rectangular 4x3 aspect-ratio set — the
  standard "country flag" presentation — filtered to the 249 files whose name is an official ISO
  3166-1 alpha-2 country code; the repository's other 8 two-letter files in that directory —
  `cp`, `dg`, `eu`, `ic`, `pc`, `un`, `xk`, `xx` — are organisation, dependent-territory or
  placeholder flags outside ISO 3166-1 and were not copied, alongside its 3-letter and hyphenated
  regional/subdivision files such as `arab`, `gb-eng`, `es-ct`).
- **Licence**: MIT. The repository root's `LICENSE` (© 2013 Panayiotis Lipiridis) covers the whole
  repository, `flags/` included — unlike a game-assets pack, there is no `img/README`-style carve-out
  narrowing it, so the grant applies to these files directly.
- **Permitted usage**: MIT terms — use, copy, modify, merge, publish, distribute, sublicense and sell,
  with no restriction beyond keeping the copyright and permission notice, which this record and the
  copy of `LICENSE` cited above satisfy.
- **Ruling**: **COPY IN**. MIT grants this directly; no Game Content Usage Rules reasoning applies,
  because flags are not game assets and constitution X does not reach them. The pack still carries
  this record because `scripts/checks/asset_packs.py` walks every directory under
  `packages/game-assets/` regardless of why a given pack is lawful.
- **Checked**: 2026-08-30
