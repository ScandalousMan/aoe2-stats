[project]
name = "aoe2stats-providers"
version = "0.1.0"
description = "DataProvider boundary — Steam, Relic, aoe.ms and aoe2companion, each behind an explicit contract."
requires-python = ">=3.13,<3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aoe2stats_providers"]
