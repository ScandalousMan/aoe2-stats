// Package skeleton (T003). No component is exported yet: design tokens and the Storybook
// configuration land in T016, the first component and its story in T035. See
// `.claude/skills/design-system/SKILL.md` for the eight-point checklist every component
// added here must satisfy before it exists.
export {}
