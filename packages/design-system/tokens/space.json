{
  "$comment": "Spacing scale (T016). `unit` feeds Tailwind's own --spacing multiplier, so every numeric utility (p-3, gap-4, ...) derives from this one value instead of Tailwind's built-in default. `scale` names the same steps explicitly for the rare non-utility consumer (inline style, canvas). Edit here, then run `pnpm --filter design-system tokens:build`.",
  "unit": "0.25rem",
  "scale": {
    "0": "0rem",
    "px": "0.0625rem",
    "1": "0.25rem",
    "2": "0.5rem",
    "3": "0.75rem",
    "4": "1rem",
    "5": "1.25rem",
    "6": "1.5rem",
    "8": "2rem",
    "10": "2.5rem",
    "12": "3rem",
    "16": "4rem",
    "20": "5rem",
    "24": "6rem",
    "32": "8rem"
  }
}
