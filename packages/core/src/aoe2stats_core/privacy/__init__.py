"""GDPR use cases: export (T090), erasure (T091, not yet written) and the third-party objection's
own instrument (T092, not yet written). Pure logic only — see `export.py`'s module docstring for
why this package holds no SQL and no object-store call, the same split `alerting.py` already draws
one level up in this package.
"""
