# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage
dist/
build/
*.egg-info/

# Node
node_modules/
.pnpm-store/
storybook-static/
playwright-report/
test-results/
*.tsbuildinfo

# Generated by @tanstack/router-plugin from apps/web/src/routes/** — never hand-edited
apps/web/src/routeTree.gen.ts
apps/web/.tanstack/

# Vercel
.vercel/

# Environment and secrets
.env
.env.*
!.env.example

# Local scratch — replays are large and must never be committed
tmp/
scratch/
*.aoe2record
*.zip
!tests/fixtures/**/*.zip

# T012b: the publication-delay corpus lives in the nightly workflow's chained GitHub Actions
# artifact (`publication-delay-corpus`), never in the repository — this is the local working copy
# a nightly run restores it into, or a developer accumulates by hand. See docs/data-sources.md §2.
docs/data-sources/publication_delay_samples.jsonl

# OS / editor
.DS_Store
.idea/
.vscode/
